name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install ruff
        run: pip install ruff==0.8.4

      - name: Ruff check
        run: ruff check app/ main.py --output-format=github

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-asyncio pytest-cov

      - name: Run tests with coverage
        run: python -m pytest tests/ -q --tb=short --cov=app --cov-report=term --cov-report=xml

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage
          path: coverage.xml
          retention-days: 7

  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Build image
        run: docker build -t jacaranda:ci .

      - name: Smoke test container
        run: |
          SECRET=$(python -c 'import secrets; print(secrets.token_urlsafe(64))')
          docker run -d --name jac -p 8000:8000 \
            -e DATABASE_URL=sqlite:///./jacaranda.db \
            -e SECRET_KEY="$SECRET" \
            -e ADMIN_PASSWORD=admin1234 \
            jacaranda:ci
          # Wait up to 90s (30 attempts x 3s) for Alembic migrations + startup
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:8000/health 2>/dev/null; then
              echo ""
              echo "healthy after ${i} attempts"
              docker logs jac | tail -20
              exit 0
            fi
            sleep 3
          done
          echo "=== container did not become healthy ==="
          docker ps -a
          echo "=== container logs ==="
          docker logs jac
          exit 1
